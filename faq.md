---
order: 50
label: FAQ
---

## Where can I find the data in Application Insights?

You can find the lag metrics in Application Insights in `Monitoring` > `Logs` in the table `customMetrics`.

## Where can I find the data in the Log Analytics Workspace?

You can find the lag metrics in the Log Analytics Workspace in `Logs` in the table `AppMetrics`.
